return function ( cfg )
  cfg.window_decorations = 'RESIZE'
  cfg.enable_wayland = true
  cfg.use_resize_increments = true
  cfg.window_padding = {
    left = 0,
    right = 0,
    top = 0,
    bottom = 0,
  }
end
