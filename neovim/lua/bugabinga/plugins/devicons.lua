return { {
  'nvim-tree/nvim-web-devicons',
  opts = {
    -- instead of creating a highlight for every single icon, use only the
    -- DefaultDevIcon highlight for all
    color_icons = false,
  },
}, }
