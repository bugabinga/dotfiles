-- TODO: create preview function, that highlights the current buffer with highlights of the global namespace, if group
-- name matches somewhere in the buffer
-- TODO: create user command for preview
local render = function () end
local setup = function () end

return {
  setup = setup,
  render = render,
}
